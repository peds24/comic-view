__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
.pytest_cache/
.claude/
.DS_Store

# Personal data — generated by scanning your own collection.
# data/library_comics.json and data/library_manga.json ARE tracked — they're
# the live library going forward. Superseded exports under data/archive/
# stay out of git except their .json snapshots (kept for reversibility).
data/covers/
data/*.csv
data/*.xlsx
data/archive/*.csv
data/archive/*.xlsx

# Real credentials — copy config.example.yaml to config.yaml and fill in
config.yaml
